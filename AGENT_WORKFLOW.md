# PINN 故障诊断系统 — Agent 工作流

本项目基于物理信息神经网络（PINN）构建轴承故障诊断系统，支持 CWRU（西储大学）和 YSU_V 两个数据集，实现 MATLAB 数据预处理、模型训练、在线诊断的完整工业故障诊断流程。

## Phase 1: 系统架构重构

初始状态是一个 Gradio 单体应用，英文界面，训练时阻塞 UI，没有实时反馈。Agent 决定将系统重构为 FastAPI + React 分离架构。后端使用 FastAPI 提供 REST API 和 WebSocket 实时训练推送，前端使用 Vite + TypeScript + TailwindCSS 构建，配合 Framer Motion 实现 Apple 风格的动画效果，Recharts 绘制实时训练曲线。WebSocket 回调最初使用 asyncio.run_coroutine_threadsafe 在 Windows 上失败，是因为 Windows 的 ProactorEventLoop 不支持该方法，改用线程安全的 queue.Queue 加主循环轮询模式解决。最终实现了三步工作流：检测数据集、输入类别名、训练模型、故障诊断。

## Phase 2: 数据泄露修复

用户报告验证集准确率异常高，Agent 分析 MATLAB 预处理代码后发现三个数据泄露问题。第一个问题：原始脚本将同一轴承信号的连续窗口段随机打乱后按 70/15/15 划分训练、验证、测试集。由于相邻窗口高度相关，这种随机打乱导致训练集和测试集之间存在严重的数据重叠。修复方案是改为时间切分：将原始信号按时间顺序切为三段，前 70% 用于训练，中间 15% 用于验证，后 15% 用于测试，各段独立生成 CWT 图像，仅在各自集合内 shuffle。第二个问题：YSU 数据集的 PCA 降维在全量信号上拟合，使得测试集信息通过 PCA 基向量泄露到训练过程中。修复方案是仅在训练段上拟合 PCA 基向量和归一化参数，再应用到验证段和测试段。第三个问题：原始脚本使用 step=1024（零重叠）导致每类仅能生成约 117 个样本，样本量偏少。将 step 改为 512（50% 重叠），每类可生成约 239 个样本。由于时间切分已经保证了集合独立性，集合内部的窗口重叠不会造成跨集合的数据泄露。

## Phase 3: 诊断流程修复

用户上传 CWT 图像后诊断完全错误。Agent 进行端到端排查：首先确认模型本身训练正确，测试集准确率达到 98.46%；然后确认数据格式正确，维度转换和值域归一化没有问题；接着模拟 PNG 转换路径，发现从原始数据到 PNG 再加载回来的误差极小，说明转换流程正确。最终定位到根本原因：Python 的 CWT 实现（pywt 的 morl 小波、ssqueezepy 的 morlet 小波）与 MATLAB 的 CWT 实现（amor 小波，48 voices/octave）输出的时频图不同。即使是同一段信号，两种实现生成的 CWT 图像差异明显，模型无法识别 Python 生成的图像。解决方案是明确约束诊断图片必须使用 MATLAB 生成，并在诊断页面添加提示说明 CWT 参数要求。同时提供了 gen_test_image.m 脚本用于从原始信号生成独立的测试图像。

## Phase 4: UI/UX 优化

用户提出三个体验问题：训练完成后不应自动跳转到诊断页面，应该先查看训练结果和模型参数；加载已有模型的功能应该放在诊断模块而非训练模块；模型文件名应包含时间戳便于识别。Agent 对训练页面进行了重构，移除了加载已有模型的区域，训练完成后停留在当前页面展示完整的训练结果，包括模型架构信息（参数量、模型大小、卷积类型、是否使用自注意力）、训练结果（最佳轮数、最佳验证准确率、测试准确率），以及混淆矩阵和分类报告。诊断页面新增模型选择区域，用户从下拉列表中选择检查点文件，加载后展示详细的模型信息（训练时间、设备、通道数、类别数、验证准确率、参数量、模型大小、类别名称列表），然后才能进行诊断。检查点文件名改为包含时间戳的格式：数据集名_YYYYMMDD_HHMMSS_best.pth，列表 API 同时返回文件的创建时间。训练完成后不再自动跳转，用户手动切换标签页。

## Phase 5: 打包分发

用户要求将系统打包为独立的可执行文件，无需安装 Python 环境。Agent 创建了 main.py 作为打包入口脚本，自动检测 frozen 状态、初始化目录、注册路由、启动服务并打开浏览器。使用 PyInstaller 的 onedir 模式打包，因为 onefile 模式需要每次启动时解压 4GB 以上的文件到临时目录，启动时间过长。打包过程中经历了多次迭代修复：排除 torch.distributed 导致 PyTorch 的 DataLoader 导入失败，排除 matplotlib 导致 utils.py 导入失败，排除 plotly 同样导致导入失败，PySide6 和 PyQt5 两个 Qt 绑定冲突需要排除一个。每次修复后重新打包约需十分钟。最终生成的 .exe 文件约 72MB，但 total 文件夹约 4.9GB（PyTorch CUDA 占了主要体积）。端到端测试确认 .exe 能正常启动、API 正常响应、前端页面正常加载。

## 关键教训

Python 和 MATLAB 的 CWT 实现差异显著，同一信号的不同库输出不同，模型训练和诊断必须使用同一管线。时间序列数据中相邻窗口高度相关，必须按时间顺序切分训练、验证、测试集，不能使用随机打乱。PCA 降维属于全局操作，必须仅在训练集上拟合参数，才能保证验证集和测试集的独立性。PyInstaller 打包大型深度学习应用时，排除不必要模块可以缩减体积但需要反复测试确保没有遗漏关键依赖。WebSocket 在 Windows 上的 asyncio 操作与 Linux 有差异，使用线程安全的队列机制更可靠。
